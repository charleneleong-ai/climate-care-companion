[project]
name = "climatise-predictors"
version = "0.1.0"
description = "L1.5 heatwave prediction seam."
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/predictors"]
