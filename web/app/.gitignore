# Both forms: the trailing slash matches a directory but not a symlink of
# the same name, which is how a node_modules symlink was once committed.
node_modules/
node_modules
.next/
out/
build/

.env
.env.local
.env*.local

*.log
npm-debug.log*

.DS_Store
.vercel
next-env.d.ts
tsconfig.tsbuildinfo
